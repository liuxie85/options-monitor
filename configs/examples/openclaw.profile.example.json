{
  "config_key": "us",
  "accounts": ["lx", "sy"],
  "max_run_age_minutes": 60,
  "paths": {
    "report_dir": "output/reports",
    "state_dir": "output/state",
    "shared_state_dir": "output_shared/state",
    "accounts_root": "output_accounts",
    "runs_root": "output_runs"
  },
  "cron_jobs": [
    {
      "name": "options-monitor auto tick",
      "id": "replace-with-openclaw-cron-id",
      "schedule": "*/10 9-16 * * 1-5 @ America/New_York"
    }
  ]
}
