#!/usr/bin/env bash
set -euo pipefail

ROOT="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
VPY="$ROOT/.venv/bin/python"

if [[ -x "$VPY" ]]; then
  exec "$VPY" "$ROOT/scripts/cli/om_agent_cli.py" "$@"
fi

exec python3 "$ROOT/scripts/cli/om_agent_cli.py" "$@"
